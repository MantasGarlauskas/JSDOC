/**
 * 
 * @param {string} name Zmogaus vardas
 * @param {string} word Skainy ka nori pasakyti
 * @returns 
 */

function chat(name, word) {
    return `${name} noreu pasakyti: ${word}`
}

module.exports = chat;